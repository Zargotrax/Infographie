--1. Si on drag une forme et qu'on touche la barre du haut du menu d'option des formes on déplace les deux en même temps--
2. Si on selectionne une forme, qu'on modifie sa couleur, qu'on crée une nouvelle forme et qu'on re-sélectionne la première,
alors cette dernière change de couleur alors qu'elle ne devrait pas
--3. Les color pickers du menu des formes glitch parfois et en selectionnant la couleur du haut on choisi celle du bas--
--4. Clicker sur le header cache le menu des fichiers--
--5. Aggretate the mesh of the imported geometry object cause now we only use the first mesh tot draw the bounding box--
--6. Le curseur de la vue 2d ne s'affiche pas par dessus le hidden UI--
--7. Le rendu du wireframe des primitives 3d (cylindre et cube) sont rendu en shader mode alors que le mode de rendu est en wireframe--
--8. Undo proportion modification isnt working--
--9. Children bounding box not applying parent transform--
--10. add trycatch for both import 2d - 3d--
--11. fix delete btn in 3d view--
--12. the cursor disapear over the view menu in the 2d view--
--13. shader mode : primitive appear all black when there should be light--
--14. Cursor is not displaing export animation when exporting in 2d view--
--15. add btn to change img layer--
--16. 2d view changing color changes all img color at the same time--
--17. ofxAssimpModelLoader: getMesh(): mesh id 1 out of range for total num meshes: 1--
--18. when adding a cylinder the menu turns black--
--19. investigate memory leak--

20. Applying filtering to a png texture with transparency isnt working
21. white wireframe? check shaders
22. textures on teapot?